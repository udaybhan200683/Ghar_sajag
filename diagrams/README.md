# Diagram reading rules

sequences/png contains 24 retained Base target sequences and 5 AI scenario images. sequences/uml contains their editable PlantUML sources. modules has the 32 baseline responsibilities and 7 additional responsibilities. architecture contains the whole-system topology.

A diagram describes ownership and order, not completion. Read the v2.0 feature explanation for guards and current code gaps. AI admission PNG lists alternative outcomes; they are not all executed in one request. Planned H10/H11/B09/A06 have no live adapters. Source layout may render differently in PlantUML; logical relationships are the intended editable content.
